---
clusters:
  - name: my-eks-cluster
    provider: aws
    region: us-east-1
    tags:
      env: dev
    nodegroups:
      - nodegroupName: my-eks-nodegroup
        scalingConfig:
          minSize: 1
          maxSize: 3
          desiredSize: 1
        tags:
          env: dev
